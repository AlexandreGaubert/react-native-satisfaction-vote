import React from 'react'

const Header = () => (
    <div style={styles.container}>
    </div>
)

const styles = {
  container: {
    height: "80px",
    width: "100%",
    display: "flex",
  },

}

export default Header
